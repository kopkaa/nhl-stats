query GetTeamGames($teamId: Int!, $limit: Int) {
  teamGames(teamId: $teamId, limit: $limit) {
    id
    gameDate
    gameState
    venue
    homeTeamId
    homeTeamName
    homeTeamLogo
    awayTeamName
    awayTeamLogo
    homeScore
    awayScore
  }
}
