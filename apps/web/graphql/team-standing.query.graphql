query GetTeamStanding($teamId: Int!) {
  teamStanding(teamId: $teamId) {
    wins
    losses
    otLosses
    points
    gamesPlayed
    divisionName
    divisionRank
    conferenceRank
    streakCode
    streakCount
  }
}
